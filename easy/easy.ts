/**
 * @param {number} red
 * @param {number} blue
 * @return {number}
 */
var MaxHeightOfTriangle = function (red, blue) {
  function dfs(red, blue, deep) {
    if ((red < deep) && (blue < deep)) {
      return 0
    }
  }
};

MaxHeightOfTriangle(2, 1)
